# 旧 Python 售后看板归档

本目录保存 2026 年 8 月 Next.js 看板接管之前的实现，只用于历史口径、迁移依据和故障复盘。

归档内容包括：

- `crm_server.py` 与 `crm_mysql_data.py` 旧 Web 实现。
- 旧 Python Web 的启动脚本和单元测试。
- 旧 Nginx 稳定版/候选版灰度配置。
- 旧 Python 看板、静态发布和 WPS 周报 launchd 配置。
- 旧周报执行脚本及当时的运维、口径和移交文档。

截至 2026-08-27，服务器实际安装的售后相关任务只有现役 Next.js 看板和班牛日更任务。这里的服务配置未安装、未运行。

## 使用限制

- 不参与 `pnpm verify`、Next.js 构建或生产发布。
- 不继续修复，不增加兼容逻辑，不恢复旧 `/crm` 路由。
- 不以旧 Python Web、旧 Nginx 或静态页面作为当前系统回滚方案。
- 当前 Web 回滚只使用上一版 Next.js standalone 发布目录。
- 历史文档中的端口、路径、账号体系和命令均可能失效，执行前必须以现役运维文档为准。

保留归档是为了保存迁移证据，并不表示这些路径仍受支持。

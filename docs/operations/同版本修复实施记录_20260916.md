# 0.6.1 同版本修复实施记录

最新状态见 [MVP 候选交付状态](MVP候选交付状态_20260916_2330.md)。**售后尚未切换生产，最终线上验收尚未完成**。下文保留 20:30 及更早阶段记录；其中微信暂停、旧构建摘要、员工入口未验收等不再代表当前状态。更早历史继续暂缓，不阻塞五周优先实施。

## 发布范围与固定口径

按用户最新要求，先发布最近五个完整周，以及现有周环比、产品周选择器和同进度功能实际需要的期间。以本轮 2026-09-16 为终点：

- 主展示：2026-08-09 至 2026-09-12，上海时区周日至周六。
- 产品页允许选择上述任一周，并保留向前五周列及其前一周比较，因此必要计算范围从 2026-07-05 开始。
- 同进度：2026-09-13 至 09-15，与 09-06 至 09-08 比较；终点均排他。每次任务按实际上海日期滚动计算。
- 原件追溯不受报告下界限制：近周售后可能对应更早订单，查询按订单身份而非创建日期截断。
- 更早原始数据、版本和备份保留。未核验旧周不能显示为真实零，也不能使用旧错误算法；旧周选择选项保留。
- **更早历史核验不作为这次发布的前置条件。**

问题按源工单创建时间归周。一条当前有效售后商品明细计一次；不乘购买数量、不合并不同工单。A×3、A×6、组合装是独立完整销售规格。分母使用同期付款且有真实发货证据的原销售行，销量使用购买份数、订单数按真实订单身份去重。已发货后退款保留原销售事实。

同期问题数/同期销售订单数是期间指标，不代表同一批订单最终发生售后的概率。经营分类、剔除规则和预警阈值保持原规则。2026-09-15 前用班牛，之后用新工单；更早的新接口迁移数据只用于核对。

子订单填成标题或主订单号的特殊放宽规则尚未获确认，仍执行“显式子单不匹配不退回主单猜填”。

## 实施位置与生产边界

| 对象 | 位置或状态 |
| --- | --- |
| 本地源码 | `/Users/hk/Documents/售后工单/aftersales-crm` |
| 修改前源码备份 | `/Users/hk/Documents/售后工单/backups/aftersales-crm-before-20260916.tgz` |
| 远端候选 | `/Users/Shared/ecom-profit/projects/crm-candidate-061-spec-fix` |
| 候选报告库 | `crm_candidate_061_20260916` |
| 真实订单预览库 | `crm_native_validation_20260916` |
| 原件及审计 | 工作区 `diagnostics` 和远端候选 `diagnostics` |
| 用户可见版本 | `0.6.1`；不通过升版区分发布 |

未切换正式 Web、正式数据任务或报告指针，未向生产插入模拟工单或销量。公司现有导出器、官方接口和规范化加载器被复用；新增下载先保留原件，必要加载仅进入隔离预览库。源账号已在工单 UI 验证为管理员。源工单批量导出生成成功但浏览器下载被拦截，不记为成功取得原件；现有 JSON 接口仍继续用于核对。

## 已实现与已核验

### 工单当前事实

200 条/页、单线程、至少 7 秒间隔、固定终点、真实空页和稳定分段复读。每张工单原子替换当前有效子行，保留旧原件和版本摘要。已通过源详情确认 11099、11100 不存在，候选撤销统计资格而保留原始记录。

真实源批次读取 355 张工单、358 条有效明细。两次在线读取间有真实更新，不用其声称幂等。随后对冻结的 355 张原件做两次事务回放，均为新增 0、变化 0、消失 0，业务摘要一致，事务回滚。证据：远端 `diagnostics/ticket-frozen-replay-20260916.json`。

班牛当前子集重建发现旧残留 3 行，并恢复最新原件中存在但旧事实表未保留的 4 行；旧原始行均保留。

### 原规格、订单身份与发货

- 删除基础码回退、去装数、组合成员相加及单问题样本回填路径。
- 京东从原始“商家SKUID”读取完整编码；淘宝/天猫读取原始“商家编码／外部系统编号”。
- 拼多多只使用正式自定义订单导出，不叠加对账报表。
- 原始订单证明复用现有旺店通原始订单接口，完整商品集合与原平台销售逐行核对后才证明主单完整性。最新原件读取批次 `original_20260916_164045_77e3b1cf` 已完成 230 个引用订单，全部取得原始记录。
- 原订单读取使用整批事务与运行/完成/失败记录；失败回滚部分证据，报告构建和发布均核对源批次、范围、整批状态和证据集合，不能混用失败轮次及旧证明。
- 抖音正式明细未保存发货时间时，读取保留 CSV，经文件 SHA1、主子单、商品、完整规格、购买数量一致后恢复证据。退款状态不抹去原发货事实。
- 微信正式入库主动清空了部分 `raw_order_json`。本轮计算范围内 94,487 条商品明细中 78,594 条为空，导致原逐商品发货信息无法直接读取。新增读取器从公司 READY 清单及逐文件 API 采集证据恢复，校验清单/原件 SHA256、稳定店铺、订单、商品、SKU、完整编码、数量、付款时间和版本；只派生发货证据，不把地址等字段写回规范库。全范围只读核对已完成：78,594 条全部找到完全匹配的原件，具有完整 SKU 发货证明的行从 13,879 条变为 84,585 条，增加 70,706 条；这不是新增订单或购买量。证据：远端 `diagnostics/wechat-original-shipping-20260705-20260916-r2.jsonl.gz` 及同名前缀 `.summary.json`。
- 初次恢复有 30,632 条缺中央清单引用。追查发现 63 个早期导出文件均在，部分分段采集的中央清单只保留最后一个分段引用。63 文件的 69 份保留版本全部通过原始 API 逐文件证据、详情数、原销售行及店铺验证，登记在候选 `crm_wechat_order_artifact`；未写入或修改公司原始审计表。它仅证明对应原订单版本，不代替付款期间完整性校验。
- 淘工厂供应商账号仅用于定位公司订单来源，原店铺 ID 用于销售行、订单和链接身份。本范围读取的 18,733 行对应 18,509 个原店铺订单，涉及 3 个供应商、6 个原店铺；经营范围核验仍是独立门槛。
- 旺店通组件、部分发货或补发不能自动证明原销售整行已发货。仅明确的普通销售出库、完整规格一致、去重后份数与原购买份数相等时补充分母及整行仓库归属；原始销售数量不取组件数量。

### 已取得并进入预览库的正式原件

| 来源 | 原件范围/数量 | 已确认的变化 | 边界 |
| --- | --- | --- | --- |
| 小红书 | 开店日至 09-15：9,092 包裹、9,171 商品行、9,086 原订单、9,324 份购买量 | 53 行由未发货变为真实已发货；原规格和数量不变 | 全历史原件与当前规范行逐行签名一致，已登记覆盖证明；不是本次五周销量 |
| 拼多多 | 19 店，09-01 至 09-15：71,588 行 | 2,956 条补充真实发货，5 条付款时间变化，编码无变化 | 正式加载器仅写预览库 |
| 拼多多 | 19 店，07-01 至 08-31：301,599 行，重复 0 | 383 条补充发货；完整编码、购买份数和付款时间未变 | 逐单旧/新差异保存在 `pdd-native-diff-julaug-20260916.jsonl.gz` |
| 微信 | 已保留首店完整读取、第二店部分响应；大范围读取已暂停 | 首店与 57 个具体缺口已完成候选更新，新增 2,067 条原销售行真实发货资格 | 未改正式库；更新时间上界导致的漏读已实证，来源完整性仍未全部通过 |

拼多多创建窗口原件已用于证明其中原订单的完整商品集合，**不能仅凭该创建窗口直接宣布整个付款窗口已完整**。

### 报告与发布

总览、产品、问题、仓库、API、分页、下钻和 Excel 固定同一不可变批次，问题日期统一。分母按具体规格/链接标记未知，不让一个未知规格无差别抹去其他已证明规格。真实零与未取得、采集失败、归属不明分开。

就绪检查区分“来源未完整或期间未计算”与“完整来源中仍有记录级待匹配”：前者阻止上线，后者保留相应的 `—`。周日没有完整日的同进度不当成采集失败。

代码指纹、规则快照、源工单批次、原订单证明批次和审计产物哈希共同参与发布校验。计算中更改代码或发布前源批次失败均阻止选择候选。前端版本号不变，构建 ID 与产物哈希待最终候选构建时重新记录；之前构建摘要不代表最终产物。

## 对账批次与未完成工作

全历史诊断 R4：`report_20260916_143614_4f05a536` 已完成，但未通过发布门槛。其原 34、43、11 条和 2,547/310 聚合组记录级对账位于远端 `diagnostics/cohort-reconciliation-r4.json.gz`。这些数量仅是旧调查集合，不能作为修复目标值。

最近范围 R5：`report_20260916_165048_3a29b379` 已完成，范围 07-05 至 09-16（排他），31,479 条问题事实。它是阶段对账批次，不包含随后新增的微信原件发货恢复和更严格的旺店通整行证明，不可作为最终发布批次。原 34 条中 11 条按真实子单、1 条按已证明的完整唯一规格归属，21 条显式子订单不匹配，1 条整单完整性未证明；原 43 条中 37 条真实子单归属、4 条子单不匹配、2 条店铺身份未证明。逐记录结果位于远端 `diagnostics/cohort-reconciliation-r5.json.gz`。

R6：`report_20260916_172111_deed05c7` 已完成，包含上述微信恢复、旺店通严格发货证明及计算脚本指纹，未发布。必要计算范围经营问题 10,104 = 已归属 9,796 + 待匹配 308；五周主展示经营问题 3,378 = 已归属 3,235 + 待匹配 143。它尚未包含随后取得的最新平台付款原件，不能作为最终发布批次。微信大范围读取已暂停并保存检查点；首店及具体 57 个缺口另已核验入候选，不能把这些结果误称为全部来源的完整验收。

R5/R6 来源覆盖检查暂有 4,006 个店铺日缺正式证据、996 个店铺日尚未证明付款窗口、17 个店铺日源审计阻塞。这是含停用/旧身份条目的保守登记检查，**不是实际缺失订单天数**。仍需对实际经营身份/有效日期、正式原件和生命周期完成核验。旧历史不在本次发布范围内的缺口不会阻塞本次发布。

还必须完成：

1. 当前范围的真实来源覆盖、原件恢复和晚更新核对；保留每个未解决记录的具体原因。
2. 新原件纳入后重建最终候选，完成 34/43/310/2547 集合和所有同类记录的记录级差异与数量守恒验收。
3. 候选端口通过实际员工入口，验证所有页面、筛选、分页、下钻及 Excel 与同批次一致。
4. 完成应用、任务和数据批次一起回滚的依据及演练，再切换正式应用和任务。
5. 验证下一轮计划任务能获取晚修改工单、晚发货订单并重算受影响周；五周稳定后继续处理更早保留历史。

## 检查记录

最新完整 `pnpm verify` 已通过类型检查、ESLint、44 项 Vitest、92 项 Python 测试和 Next.js standalone 构建。构建 ID `crm061-2ccfee2c33621ac2a6af`，standalone 树 SHA256 `5504cd467f3b5335e192ce5f05ef0966f8250b356cf942f23ec5111363c5a63c`。用户可见版本仍为 `0.6.1`。构建记录：工作区 `diagnostics/candidate-build-r8.json`；检查日志：`diagnostics/candidate-verify-r8.log`。真实数据、员工入口和线上发布验收尚未通过。

原文早期实施记录保留于工作区 `diagnostics/implementation-record-before-scope-20260916.md`，其中旧阻塞状态、旧测试数和构建摘要不代表当前状态。

## 付款原件后续核验（18:55 阶段记录）

用户提出争取 19:00 上线后，明确选择“坚持五周及比较期全部核验后上线，时间可以延后”。不采用问题先上线、缺分母全部显示破折号的部分发布方案，来源完整性门槛保持。

- 快手人工滑块已完成，正常订单读取和正式付款报表导出已恢复。榆园食品专营店（158190538）官方付款报表 33,604,805：71,523 条订单，与规范库订单集合完全相同，SKU、购买份数、付款时间未变；10,185 条状态变化、458 条发货时间变化。原件及逐条差异已保留，71,523 条仅导入隔离预览库，未改生产。其他实际店铺付款报表按源平台间隔限制继续生成。
- 抖音当前账号 15 店的付款报表均已取得，不重复导出。每份绑定稳定店铺、源任务 ID、源查询条件、原件 SHA256 与源订单总数。榆园儿食品专营店（10461048）473,234 商品行、471,795 原主单：与旧库集合一致，完整编码、商品、份数和付款日期未变，84,737 条状态变化；已通过预览导入。其他店铺逐个对账，不把这个结果推广为全部店铺无变化。
- 导入器按源查询的付款时间校验，订单创建时间保留原值，不以创建期间丢弃跨期付款。保留其他业务已有的创建时间导出用途，但不据其审计日期直接证明 CRM 付款分母。
- 新付款覆盖检查要求原件逐行匹配规范库，并核对平台较早创建订单入口。较早入口有付款订单时必须取得对应原件，不能因本期创建订单完整就判定本期付款完整。该检查只针对本次付款区间，不扩展旧报告核验范围。
- 微信东北美食盒子首批 39,290 个候选订单全部取得详情，39,359 商品行，失败 0；其他店铺继续获取。需要随后完成规范库对账与读取期间晚更新闭合，尚不记为付款覆盖通过。
- 新增回归覆盖跨期付款、完整组合规格、不重复子单、缺失购买数量、发货后退款、历史创建入口未闭合、最新来源失败不能复用旧成功证明。当前 Python 数据测试 84 项通过；完整 Web 构建和真实页面验收仍待最终数据批次，旧构建摘要不代表新增数据脚本。

候选的 native 预览视图只用于读取隔离原件与其他现存真实记录。正式发布必须消除这类预览拼接，切到经过验收的正式来源及不可变报告批次；不把固定日期预览覆盖作为长期任务机制。

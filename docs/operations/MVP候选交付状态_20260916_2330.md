# 五周 MVP 候选交付记录

更新时间：2026-09-17 00:30，上海时区。**售后正式页面及正式数据任务仍未切换。** 本记录承接 22:50 阶段记录，不能当作正式上线验收书。

## 最终上线判定

本次候选不发布，正式入口继续使用稳定版 0.6.1。候选批次 `report_20260916_232514_73aed353` 在真实员工账号、公司统一入口和 R13 构建下完成页面验收后，经营总览、产品分析中的销售订单、销量和售后率全部显示为“—”，页面提示 3,952 条缺分母。该结果没有解决本次交付要求，属于上线前发现的发布级问题。

发布门槛按设计拒绝该批次，记录为 `accepted=false`、`production_published=false`、`current_pointer_unchanged=true`，原因是店铺、日期和付款窗口覆盖尚未闭合。没有绕过门槛，也没有把候选批次写成正式当前批次。

R12/R13 的其余验收已经完成：

- 独立复算读取 2,083,616 条销售行，销售行身份重复为 0；其中 1,913,790 条具备可计入的真实发货证据，12 个必需期间和 22,606 个聚合分组无差异。
- 34 条原待匹配、43 条原自动补码、11 条唯一组合优先集合完成回归；明确子订单错误继续保留待匹配，未退回主订单猜填。
- 六个真实原单案例及三张工单旧子行撤销复核通过；候选分母因来源覆盖未批准而保持 `verified=false`。
- 五个完整周的产品分页、明细分页和 10 个 Excel 逐值一致。各周产品／明细行数依次为 283/736、221/497、201/398、187/339、180/289。
- 完整周总导出含 1,735 条明细和 180 个产品，最新周产品问题合计 343；同进度总导出含 366 条明细和 55 个产品，产品问题合计 99。工作表与接口逐单元格一致。

候选浏览器选择已清除并恢复普通正式入口。正式应用软链接仍为 `/Users/yyerybz/Codex/aftersales-dashboard-0.6.1-20260915T174925`，LaunchAgent 仍运行原进程；正式页面仍显示原稳定数据。由于切换从未发生，不需要执行正式回滚。已验证的数据备份和旧应用隔离启动结果继续作为后续发布的回滚依据。

主展示范围仍为 2026-08-09 至 2026-09-12。仅保留现有比较入口所必需的支撑期间及本周同进度；不开展更早报告问题的全面清理。原件为本期问题归属所需时可追溯更早订单。

## 本阶段完成

- 工单原单核查批次 `original_20260916_224107_f020b1f3` 完成 230/230，精确查询空结果为 0，与 `ticket_20260916_222937` 绑定。未重新猜填错误子订单号。
- 京东实际采集控件在目标起始日期晚于旧结束日期时，原来先设置起始日期会使控件内部日期范围失效。现改为该情形先设置结束日期，并保留查询回读和生成任务验证。两店 09-01 至 09-15 均实际生成并下载新正式报表。原脚本已备份，变更仅这一日期设置顺序。
- 京东两店三个连续导出窗口覆盖所需创建日期范围。6 份加密官方原件保留；2,556 条规范明细与原件验证记录、购买份数、付款、出库和状态逐条核对，重复键、同键冲突、迟到覆盖均为 0。支撑范围付款行 2,553；其中最近五周付款行 1,168。386 条记录变化中，3 条是原有行补充出库证据。创建窗口已对账不等于付款窗口已经证明完整。
- 京东候选视图按 `shop_id + main_order_id + product_id` 选取上述已审计版本；先检查原始身份唯一性及正式库没有更新的业务版本。正式订单事实未写入。
- 天猫榆园食品专营店 `106263748` 的 20:57:39 正式报表已生成并下载。文件 SHA256 为 `7cfe02b6e921ccbe01ba8637a2772118098acbb744b936adcce07b735e68d68d`。227,140 条商品明细全部付款于 07-05 至 09-15；215,890 个主订单与平台付款筛选结果完全一致。最早创建时间 07-04 22:04:53，跨期创建、期内付款未截断。
- 该天猫原件已通过公司现有加载器写入隔离预览库，并进行双向商品行核对。新增 2,917 条销售明细，5,273 条已有明细补充发货证据，共 8,190 条影响销售事实；最近五周内有 3,376 条。保留每条原值、新值、原件哈希和行号。无版本冲突或迟到覆盖。未把平台旧订单页的覆盖问题直接标为通过。
- 为缩短重算时间，抖音正式及预览明细增加 `(shop_id, paid_date)` 在线索引，不改变业务事实。微信原件解析缓存从 8 个文件增至 32 个，仍以路径、原件摘要和 inode/大小/修改时间绑定，保留全部原件验证。
- R11 旧计算 `report_20260916_225629_d93e5311` 在发布前停止并标记失败，原因是新京东和天猫原件在其一致性快照之后取得。中间产物和取消原因保留。没有将该半成品设为当前批次。
- R12 新候选计算已完成，采用上述最新销售原件；批次为 `report_20260916_232514_73aed353`。独立销售复算、历史回归集合、真实原单案例、全分页和 Excel 验收结果见本记录“最终上线判定”。来源覆盖门槛拒绝发布，正式当前批次未变。
- 淘工厂先前提出的店铺归属问题已从正式原件查明，无须用户凭记忆确认：08-22 订单 `3316374986163008250` 出自公司已登记的沈阳乔希商贸有限公司供应商账号，官方文件明确记载店铺 `2222543647507`、规格 `2026071302*2`、购买 1 份。源查询为账号全部可见店铺，1,200 条商品明细对应 1,197 个主订单，与页面总数一致。官方清单、查询及原件 SHA256 均核验，数据库该商品行的身份、规格、数量、状态和时间与原件一致。证据 `tgc-current-storefront-r1.json`。这解决了该条归属证据问题；报告覆盖程序仍需接入该证明，不能据此宣布整个付款期间已核验。

## 构建和验证

最新 Web 候选为 R13，版本仍为 **0.6.1**，构建 ID `crm061-d6e74ded169415df0f1a`。本次补充修复“同进度完整导出”的产品工作表原来仍使用上一个完整周的问题。产品工作表现从同文件已筛选的发生期明细生成，保留工作表和字段形式；产品页面组件与修复前备份 SHA256 相同。

真实 R6 数据的修复前导出复现该问题：本周同期应为 09-13 至 09-15，产品工作表却标为 09-06 至 09-12。修复后，同进度的 366 个明细分组与接口逐单元格一致，55 个产品规格的所有数值与同文件明细一致，已归属问题合计 99；完整周的 1,735 个明细分组、180 个产品规格也通过核对。R12 新原件批次又独立完成五个可选周的全部产品／明细分页、10 个对应 Excel 及完整／同进度总导出逐值验收。来源覆盖仍未通过，因此这些候选分母没有发布。

R13 类型、ESLint、45 项 Vitest、98 项 Python 和 Next.js 构建通过。standalone 2,123 个文件，清单 SHA256 `ba0f4781ecd7e134ef8475eda3997074e1be6b7cb64fe03f603e0cc5df478c17`；压缩包 SHA256 `52ba2f2351ba61515129c3a2647db5e1a7ecbfd2119f37d5ccfef531bdf6684d`，本地与远端一致。18096 已换为 R13，固定等候 `report_20260916_232514_73aed353` 完成。原正式 18091 及其软链接未改。

新数据重算没有因这项 Web 修复重启。限定该批次的后续验收已执行独立销售复算、34/43/310 回归集合及同类记录对账、真实原单案例、全分页与导出和只读发布门槛验证。发布门槛拒绝批次并保持正式指针不变。结果保存在远端 `diagnostics/acceptance-r12-r13/` 及本地 `diagnostics/xlsx-audit/acceptance-r12-r13-http-final-*.json`。

以下保留 R12 构建记录：

售后版本继续为 **0.6.1**。R12 构建 ID：`crm061-07e5c0382daf8d51388b`。类型、ESLint、44 项 Vitest、98 项 Python 测试和 Next.js 构建通过。

standalone 2,069 个文件；按相对路径、类型、内容哈希排序的 JSON 清单 SHA256：`0a9e9afbb308451ab6ef825dd965576ed99f8619366b71a2157a2d3546526753`。传输压缩包 SHA256：`82add71e1b1e3229c88cbab541bd20760ab0ddae46c0aab0a022a703458aa380`，本地和远端一致。清单算法已写入构建元数据，不与其他摘要算法混比。

公司网关当前为 4.8.70；普通售后入口仍指向原 18091。实际员工候选验收以及真实规则变更、5 周 API/Excel 核对见 22:50 记录，使用 R11 程序与 R6 阶段数据，不能替代 R12 新数据验收。旧管理员候选 cookie 已清除。

## 回滚准备

正式 CRM 9 张表的一致性备份已成功恢复到隔离库 `crm_rollback_acceptance_20260916_2302`，逐表行数和有序记录 SHA256 与正式库完全一致。包含规则、源状态、仓库映射、旧日周汇总、工单批次及原始记录；原件、凭据和实际 LaunchAgent 配置另有受保护备份。

另已复制正式应用，在隔离端口 18098 配合恢复库实际启动，健康检查为 0.6.1。产品和发生周明细两条查询的完整 JSON 与备份时正式基线相同；临时 LaunchAgent 已关闭，正式指针和配置未改。此项证明原应用配合恢复数据可以启动和查询；未执行正式任务，不能称为应用、任务和数据批次的联合回滚演练全部通过。

数据任务模板已固定使用公司现有虚拟环境 Python，所需 `pymysql/openpyxl/sqlalchemy/dotenv` 已验证。模板尚未安装到正式任务；实际正式任务路径仍为原 dated release，不能把 `/projects/crm` 普通目录误当作已切换软链接。

## 尚未完成的发布条件

1. 源文件取得、逐条规范化和来源完整性是三个不同检查。当前来源完整性证明实现仅接入抖音、小红书；其余来源的付款窗口、历史创建但本期付款、晚发货更新、有效店铺范围仍须按原件证据闭合并接入发布检查。**不能只把数据库已有行匹配成功就标为整期已核验。**
2. 东方甄选 `DFZX001` 的本期查询人工验证尚未确认完成，涉及端口 9899。淘工厂该条店铺归属已由正式原件解决，原来的用户确认问题不再作为阻塞；来源整期覆盖及该证明的程序接入仍未完成。
3. R12 的回归集合、销售独立复算、全部分页、真实员工页面和 Excel 一致性验收已执行；2,547 是旧批次缺分母聚合数，候选页面实际为 3,952 条，未把任一调查数字当作目标值。当前阻塞是来源覆盖，没有用匹配成功率代替来源完整性。
4. 仍需在来源覆盖闭合后验证下一轮正式任务能够更新晚修改工单、晚发货订单及受影响周，并完成应用、任务和数据批次的联合切换及回滚演练。不得以单次构建成功代替。

上述情况如实保留。未将未知数据写零，未凑历史调查数字，未以未核验来源的“—”作为完成发布的替代条件。

## 证据定位

工作区 `diagnostics/` 下：

- `jd-mvp-preview-audit-r1.json`、`jd-preview-selection-r1.json`
- `tmall-inspection-106263748-r1.json`、`tmall-preview-106263748-r1.json`
- `candidate-build-r12.json`、`candidate-tree-r12.json`、`candidate-verify-r12.log`
- `candidate-report-build-r12-new-originals.log`
- `rollback-data-restore-r11.json`
- `rollback-application-r1.json`
- `candidate-build-r13.json`、`candidate-tree-r13.json`、`candidate-verify-r13.log`
- `complete-export-before-fix-r3.log`、`complete-export-after-fix-r13.log`
- `xlsx-audit/candidate-http-r13-regression-complete-verification.json`
- `xlsx-audit/candidate-http-r13-regression-verification.json`
- `acceptance-r12-r13.log`
- `tgc-current-storefront-r1.json`

远端 `crm-candidate-061-spec-fix/diagnostics/` 下保留对应原件、逐条变更 gzip 和上游完整验证记录。消费者信息不复制到报告。

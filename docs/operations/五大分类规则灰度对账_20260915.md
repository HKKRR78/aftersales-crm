# 0.6.1 五大分类规则灰度、对账与发布记录

## 发布结果

- 正式版本：`0.6.1`。
- 正式应用目录：`/Users/yyerybz/Codex/aftersales-dashboard-0.6.1-20260915T174925`。
- 正式数据目录：`/Users/Shared/ecom-profit/projects/crm-0.6.1-20260915T174925`。
- 生产服务：`com.company.aftersales-dashboard`，监听 `127.0.0.1:18091`。
- 生产调度：`com.ecom-profit.crm-aftersales-sync`，仍为每日 7:20、13:20、19:20。
- 回滚备份：`/Users/Shared/ecom-profit/backups/aftersales-0.6.1-20260915T174925`。

## 新统计规则

一级问题直接归入快递、库房、买家、产品、运营五类。六条可剔除规则独立存放于 `dim_crm_exclusion_rule`：

1. 快递问题 / 顾客退款 / 全部三级。
2. 快递问题 / 签收未收到 / 需派件上门。
3. 快递问题 / 改地址 / 全部三级。
4. 快递问题 / 物流延迟 / 需催件。
5. 快递问题 / 需派件上门 / 全部三级。
6. 买家问题 / 无理由退 / 全部三级。

命中规则的记录保留在全量售后和 Excel 中，标记为“可剔除”，不进入经营、产品、仓库和趋势统计。五类之外的问题标记为“待归类”，保留原始明细并暂不进入统计。

## 全历史口径对账

灰度库和正式库得到相同结果。当前来源门禁内共有 40,753 条可统计事实：

| 指标 | 数量 |
| --- | ---: |
| 旧经营口径 | 14,086 |
| 新经营口径 | 13,708 |
| 净变化 | -378 |
| 新规则可剔除 | 27,044 |
| 待归类 | 1 |

主要差异：

- 新增剔除“签收未收到 / 需派件上门”723 条。
- 新增剔除“物流延迟 / 需催件”630 条。
- 恢复纳入口感问题 537 条、口味问题 302 条、好/差评红包 67 条、返差价 29 条、购物卡退款 13 条、物流延迟要求理赔 13 条、顾客/快递补款 10 条、运营其他 4 条。
- 历史“平台介入 / 仅退款”1 条保持待归类。

差异合计与 `13,708 - 14,086 = -378` 一致。疑似重复记录没有因分类切换被合并或删除。

## 产品问题数复核

对 `2026-09-06` 开始的完整创建周逐产品比较产品页、创建周明细和分页接口：

- 产品 178 个。
- 明细组合 278 个。
- 有商家编码的产品问题数 337；产品页合计同为 337。
- 缺商家编码问题数 34，继续单独列为待匹配。
- 逐产品差异 0，排序为所选周问题数全局降序。

因此，上个版本截图中“左侧问题数与右侧任意周混淆、未按所选周排序”的问题仍保持已修复状态；本次分类切换没有重新引入该问题。

## 验证

- `pnpm verify` 通过：34 项 Vitest、19 项 Python 测试及 Next.js 生产构建全部通过。
- 灰度 `healthz`、`readyz`、设置页、分页 API 和全量 Excel 通过。
- 正式 `healthz` 返回 `0.6.1`；`readyz` 数据源新鲜、总体订单截至 `2026-09-14`。
- 正式设置页显示五类和六条可剔除规则；全量 API 当前窗口可检索到 1,575 个可剔除明细组合。
- 正式 Excel 压缩结构校验通过；旧风险字段未出现在新 API 和 Excel 中。
- 公司公网入口将未登录浏览器正确跳转到员工认证；应用直连缺少公司身份时返回 401。

商品订单水位仍停在 `2026-08-06`，所以 `readyz` 的分析状态继续为 `partial`。这属于既有上游数据缺口，不影响本次问题数量、分类和剔除规则对账。

## 回滚

应用可将 `aftersales-dashboard-current` 切回 `0.6.0` 目录并重启看板服务。调度 plist 和旧问题规则表备份均保存在本次回滚目录。旧表没有删除或改写，新表为独立新增，因此应用回滚不需要恢复售后事实数据。

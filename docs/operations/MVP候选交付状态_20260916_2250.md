# 五周 MVP 当前交付状态

本文件保留 22:50 阶段状态；后续进展见 [最新候选交付记录](MVP候选交付状态_20260916_2330.md)。

记录时间：2026-09-16 22:50，上海时区。售后正式服务尚未切换，不能据此文件宣布修复已上线。

## 范围

主展示为 08-09 至 09-12 五个完整周。保留现有周选择器向前五列及其环比所需的 07-05 起比较期间；本周同进度需要 09-13 至 09-15。更早报告问题暂缓，不以全留存历史核验作为发布条件。仅为范围内售后追查原订单时允许追溯更早原件。

## 已验证的交付物

| 交付物 | 当前证据及边界 |
| --- | --- |
| 售后程序 | 版本 0.6.1；R11 构建 `crm061-96a4419f2c0526a9a0d6`；standalone 树 SHA256 `360738944b0628a5e232c387824466cb8b203b406eec31879fc4e7180b96b2ce`，2069 文件。已在候选 18096 启动。 |
| 代码检查 | 类型、ESLint、44 项 Vitest、98 项 Python 测试和 Next.js 构建通过，日志 `diagnostics/candidate-verify-r11.log`。 |
| 真实规则变更集成 | 隔离测试库 `crm_rule_acceptance_20260916_2225` 复制真实 R6 事实；变更真实剔除规则后，5 周产品／明细／Excel 与事实逐项守恒，恢复规则后回到原值。测试库的 verified 标志仅用于验证发布分支，明确标记 `integrationTestOnly=true, sourceCoverageApproved=false`，不是来源覆盖证明。日志 `diagnostics/rule-live-integration-r3.log`。 |
| 防止批次篡改 | 工单明细集合逐项与权威当前源对照；问题、销售聚合、分母状态、店铺日期覆盖均绑定排序后的记录哈希。等行数但值改变不能通过发布。 |
| 公司入口 | 公司网关 4.8.69 修复规则 POST；4.8.70 增加按实际管理员、精确构建校验的短期候选 cookie，已部署且公共健康接口验证。普通售后入口仍指向原 18091。 |
| 真实员工验收 | 实际“杨／管理员”通过公司公网入口访问 R11，导航保持批次；原值保存规则成功。隔离规则库另外验证实质规则变化与 Excel 一致。公开入口 Excel 已触发浏览器下载事件；服务端完整导出记录成功，R6 的 1950 个导出聚合行。首次未接收下载事件的浏览器点击被客户端拦截，保留记录，不将其当作服务故障或成功下载。 |

候选 UI 目前使用阶段数据 `report_20260916_172111_deed05c7`。该数据未包含后续全部原件更新，不是最终交付批次；`readyz` 正确返回 503 / `report_not_verified`。

## 最新真实数据处理

- 工单批次 `ticket_20260916_222937`：固定读取 09-15 至 09-16 排他，355 工单、358 当前明细；两轮完整读取及终止页通过。相对上一轮真实变化 1 行（工单 10920），新增／移除均为 0。旧摘要保留。
- 快手预览合并发现同一店铺原订单的新旧导入哈希不同，导致重复候选销售行。候选视图现以 `shop_id + platform_order_id` 识别同一真实订单，先校验唯一性及新版本不早于正式库，再合并。正式源未改。审计 `native-preview-identity-r3/result.json`。严格重复拒绝逻辑保留。
- 微信 5 个实际 API 店铺的 07-05 至固定 09-16 16:46:24 更新时间原件已完整取得；22:22:48 固定上界的增量补读也完成，详情失败均为 0。公司原有规范加载器已导入销售预览库，完整原件及回滚差异保留。
- 微信逐页审计核对请求范围连续性、游标链、终止页、枚举订单与成功详情完全一致，再比较库内全部同期付款行。特色食品 27896、东北美食盒子 29942、馋味说 33156、东北特产 1134 行与本轮原件的销售身份、规格、数量、付款、版本和状态完全一致。小峰优选 2359 行中有 1 条更新移到补读上界之后；已通过该具体订单的官方详情取得 22:34 的发货更新并导入候选。源明细 `wechat-update-gap-xiaofeng-r2`，预览审计 `wechat-update-gap-xiaofeng-preview-r1`。这些数量为必要比较范围的销售行数，不是五周销量。
- 淘宝／天猫 5 店已有本轮原件及逐行预览更新。4 店新增批次：452993389（223 条有业务影响变化）、395644837（13）、556216383（61）、69799771（199）；先前 283296973 也已核对入预览。仅状态名称完成化不计算为新增销量。
- 天猫 106263748 的 20:57:39 官方报表，22:47 检查仍在生成；未重复提交大报表，保留官方任务及失败下载记录。
- 京东 2 店使用现有公司任务补读 07-05 至 09-15，关闭下载后正式入库。仅原件取得不等于付款窗口覆盖；不将创建日期查询冒充付款查询。

微信原件内 91 个旧状态冲突均位于本次付款范围之外，已保留冲突记录并暂缓，不为历史冲突扩大本次处理范围。

## 本次仍未通过的条件

1. 所有实际来源的付款覆盖证据尚未全部接入并通过最终报告验证。当前程序只为小红书、抖音接入完整原件覆盖证明；其他来源已取得或补读的原件仍须完成相应核验，不能改成功标志绕过。
2. 东方甄选 DFZX001 的近五周及比较期核验需要端口 9899 的人工滑块验证；已请求用户完成，尚未收到完成回复。
3. 部分当前经营身份仍缺确认。例如淘工厂 08-22 原订单 3316374986163008250 的来源店铺 2222543647507 在原件中真实存在，但未登记到已确认来源店铺范围。不能按标题补归属。
4. 最新工单、原订单与销售原件需要进入新报告，再完成回归集合、全分页、下钻、导出的最终一致性验收。原 34／43／310／2547 不是目标数。
5. 后续晚发货来源刷新与新日任务的依赖尚未验收；应用、任务、批次及规则共同回滚演练尚未完成。未安装新正式日任务。

任何外部阻塞解决前，都不能将本记录表述为“今晚 MVP 已上线”。已核验的候选产物、原件和证据继续保留。

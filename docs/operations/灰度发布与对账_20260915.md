# 0.6.0 灰度发布与对账记录

## 发布状态

- 正式服务：`0.6.0`，`com.company.aftersales-dashboard`，端口 `127.0.0.1:18091`。
- 正式数据库：`ecom_profit`；统一事实表已导入班牛历史 64,255 行及已验证的新接口稳定周 2,604 行。
- 交接边界：`2026-09-15 00:00:00`。班牛只统计边界前记录，新接口只统计边界起记录。
- 统一调度：`com.ecom-profit.crm-aftersales-sync`，每日 7:20、13:20、19:20；历史起点为交接日。
- 旧班牛调度已持久卸载。灰度和候选服务已停止，数据库和发布目录保留用于审计。

## Review 修复

1. 班牛历史导入改为保留全部 64,255 行；其中 40,753 行延续原 API 统计口径，23,502 行仅作历史归档证据。
2. 新接口分页必须持续到空页，不能在不足一页时提前结束。
3. 历史导入保留既有仓库名称，并兼容历史匹配状态长度、空退款时间和字符串业务键。
4. 对账重复候选按订单、订单加子订单、订单加子订单加商品三层输出，不自动合并。
5. 班牛历史导入写入成功批次，灰度水位不再依赖行级最大更新时间。
6. 来源门禁增加起止时间，交接审批在同一事务中关闭班牛后续区间并开启新接口区间。
7. 产品“问题数”固定为所选创建周，并与右侧第一周列及明细下钻使用同一范围。

## 自动验证

- `pnpm verify`：通过。
- Vitest：9 个测试文件、31 项测试通过。
- Python：17 项测试通过。
- Next.js 生产构建：通过。
- 灰度 `healthz`、`readyz`、产品页、分页 API 和 Excel：均返回 200。
- launchd 自动拉起验证：主动终止后以新 PID 恢复服务。
- 正式产品周 183 个产品、291 个问题明细组合逐项对账，问题数合计 349，差异 0。
- 正式 Excel 为有效 Office Open XML 文件，压缩结构校验无错误。
- 员工网关对未登录请求返回登录跳转；应用直连对缺失员工身份请求返回 401。

## 班牛历史对账

- 源表与统一事实表：64,255 行，逐字段差异 0。
- 可统计事实：40,753 行；归档事实：23,502 行。
- 创建周近四周新旧差异均为 0：1,812、1,200、965、103。
- 付款周近四周新旧差异均为 0：1,089、829、272、7。
- 新工单批次写入前后，灰度产品 API 响应摘要完全一致，来源门禁生效。

## 新工单稳定周批次

范围为 `[2026-09-01, 2026-09-08)`。

- 请求页数：262（包含双遍和空页）。
- 工单：2,595。
- 事实：2,604；其中 `order` 商品行 2,570。
- 新增：2,604；变化：0；缺失：0。
- 非订单行：`missing` 23、`wrong` 11，均未进入商品问题统计。
- 缺商家编码：3；缺三级问题：840；仓库冲突：0；空商品工单：0。
- 疑似重复：订单级 18 组、订单加子订单 11 组、完整三级键 8 组。

## 发布后的待验收项

1. `[2026-09-08, 2026-09-15)` 是仍在变化的交接前区间，未作为发布批次；交接日期后的首个完整日由 9 月 16 日自然调度读取。
2. 商品订单原始表只覆盖到 `2026-08-06`，正式就绪状态因此为 `partial`；总订单已刷新到 `2026-09-14`。恢复商品订单上游后再消除该状态。
3. 首个交接后完整批次及连续两个自然调度周期仍需运行证据。失败批次不会刷新成功水位，也不会删除上次完整结果。

## 回滚点

- 应用上一版本：`/Users/yyerybz/Codex/aftersales-dashboard-0.5.0-20260914T071252Z`。
- 完整发布备份：`/Users/Shared/ecom-profit/backups/aftersales-cutover-20260915T132700`。
- 数据库备份文件：上述目录内 `crm-pre-cutover.sql`，SHA-256 为 `4805925ab2b4cd1d76230dffdac631c8748835740578fa2c6c164df22efc8ce0`。
